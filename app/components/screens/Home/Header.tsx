import React from "react";
import Avatar from "../../UI/Avatar";
import Padding from "../../UI/Padding";

const Header = () => {
	return (
		<Padding className="flex-row">
			<Avatar />
		</Padding>
	);
};

export default Header;
